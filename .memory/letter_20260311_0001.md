# Letter to Myself (Session Handoff)

**Date:** 2026-03-11, ~18:00

## 1. Executive Summary
* **Goal:** Migrate the nyxCore Cognitive Vault static HTML landing page to a Next.js 15 App Router app with TypeScript, Tailwind, i18n (EN/DE), Airtable CTA, download section, interactive demo modal, and Vercel deployment.
* **Current Status:** Design spec and implementation plan are complete and committed. About to begin subagent-driven execution of 18 tasks across 5 chunks. No code has been scaffolded yet — still on static `index.html`.

## 2. The "Done" List (Context Anchor)
* Created `index.html` — full static landing page with Neo-Tokyo Terminal aesthetic (code rain canvas, holographic tilt cards, CRT scanlines, scroll reveal, terminal easter egg)
* Updated `index.html` with real product data from `../OnboardingTutor/docs/` (CKB tools, multi-provider AI, architecture diagram, security features)
* Brainstormed design with visual companion — chose: Platform Cards Grid (downloads), Browser Frame + Play Overlay (demo), Single-Column Clean (Airtable form), Single-Page + Modal Demo (architecture)
* Wrote design spec: `docs/superpowers/specs/2026-03-11-landing-page-nextjs-migration-design.md`
* Wrote implementation plan: `docs/superpowers/plans/2026-03-11-landing-page-nextjs-migration.md` (18 tasks, 5 chunks, reviewed and fixed)
* Pushed all commits to `origin/main` (3 commits: initial, prototype+spec, plan)

## 3. The "Pain" Log (CRITICAL)
* No major issues encountered yet — haven't started implementation.
* **Note:** Vercel static file limit is 100MB per file. Binaries (macOS DMG = 180MB) must be served from a separate public GitHub releases repo, NOT from `public/downloads/`.
* **Note:** `next-intl` with App Router requires specific file structure: `i18n/request.ts`, `i18n/routing.ts`, `middleware.ts`. The plan has exact code for all of these.
* **Note:** The plan's `page.tsx` is a client component (uses `useState` for form modal state). `setRequestLocale` must stay in the locale layout, not in page.tsx.

## 4. Active Variable State
* Repo: `github.com:mrwind-up-bird/landingpage-onboardingtutor.git`
* Branch: `main` (3 commits ahead of initial)
* No env vars configured yet — needed for implementation:
  - `AIRTABLE_API_KEY`, `AIRTABLE_BASE_ID`, `AIRTABLE_TABLE_NAME`
  - `NEXT_PUBLIC_DEMO_URL` (optional, shows "Coming Soon" if unset)
  - `NEXT_PUBLIC_RELEASES_REPO` (GitHub releases URL)
  - `NEXT_PUBLIC_RELEASE_VERSION` (e.g., `1.1.0`)
* OnboardingTutor source project: `../OnboardingTutor/` (docs referenced for product accuracy)
* Existing releases: v1.1.0 with macOS (.dmg 180MB), Windows (.exe 85MB), Linux (AppImage 110MB, .deb 85MB)

## 5. Immediate Next Steps
1. [ ] Execute plan Task 1: Scaffold Next.js with `create-next-app`, install `next-intl`
2. [ ] Execute plan Task 2: Configure Tailwind with Neo-Tokyo theme colors, fonts, keyframes
3. [ ] Execute plan Task 3: Set up `next-intl` with EN/DE locale routing and middleware
4. [ ] Execute plan Task 4: Create app layouts with Google Fonts (JetBrains Mono, Inter, Noto Sans JP)
5. [ ] Execute plan Task 5: Build layout shell (Nav, Footer, CodeRain, Scanlines, ScrollProgress)
6. [ ] Continue through Tasks 6-18 per plan: UI primitives, sections, Airtable, deploy
7. [ ] User needs to: create Vercel project and connect the GitHub repo
8. [ ] User needs to: create public GitHub releases repo for download binaries
9. [ ] User needs to: set up Airtable base with fields: Name, Email, Team Size, Use Case, Timestamp, Locale
